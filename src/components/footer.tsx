import styles from '../styles/footer.module.css';

const Footer = () => {
    return (
        <footer className={styles.footer}>
            <p>
                <span>React + TS + NextJS</span> @2023
            </p>
        </footer>
    )
}

export default Footer;